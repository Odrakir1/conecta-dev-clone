import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    main:{
        height:"100vh",
        display:"flex",
        minWidth:"1200px",
        width:"100vw",
        margin:"0 auto"
    },
    
})

export default useStyles;

