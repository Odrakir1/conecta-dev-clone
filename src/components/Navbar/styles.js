import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    navbar:{
        background: "blue",
        width: "350px",
        padding:"24px"
    }

})

export default useStyles;