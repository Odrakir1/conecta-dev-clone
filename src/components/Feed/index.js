import React from 'react';
import useStyles from './styles';

function Feed(props){

    const styles = useStyles();

    return(
        <div className={styles.feed}>

        </div>
    );
}

export default Feed;