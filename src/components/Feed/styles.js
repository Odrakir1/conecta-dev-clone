import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    feed:{
        background: "yellow",
        width: "100%",
        padding:"24px"
    }

})

export default useStyles;